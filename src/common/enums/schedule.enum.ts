export enum RepeatUnit {
  MINUTES = 'minutes',
  HOURS = 'hours',
  DAYS = 'days',
  WEEKS = 'weeks',
  MONTHS = 'months',
  YEARS = 'years',
}

export enum EndScheduleOn {
  CANCELLED = 'cancelled',
  COUNT = 'count',
}
